---
phase: 02-products-about-contact
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - CONTACT-01

must_haves:
  truths:
    - "The #bo-suu-tap section shows a visual gallery of all 12 product images in an asymmetric grid with no text overlays"
    - "The #ve-kytoo section contains a brand description paragraph, a UVP paragraph, and two stat numbers (1.000+ and 4.8 star) displayed prominently in kytoo-red"
    - "The #lien-he section contains a Zalo contact card and a Facebook contact card, each with an SVG icon and placeholder link"
    - "The footer shows social icon links for Zalo and Facebook above the copyright line, and the copyright year reads 2025"
    - "All new HTML uses only kytoo-* color tokens, no rounded-* classes, and SVG icons use stroke-linecap=square"
  artifacts:
    - path: "index.html"
      provides: "Gallery grid replacing line 106 stub; about 2-col layout replacing line 116 stub; contact cards replacing line 126 stub; updated footer with social links replacing line 133 copyright-only paragraph"
      contains: "id=\"bo-suu-tap\"  id=\"ve-kytoo\"  id=\"lien-he\"  1.000+  4.8  zalo-placeholder  facebook-placeholder  2025 KYTOO Store"
  key_links:
    - from: "contact section Zalo link"
      to: "#zalo-placeholder"
      via: "href attribute"
      pattern: "zalo-placeholder"
    - from: "footer social links"
      to: "#zalo-placeholder and #facebook-placeholder"
      via: "href on <a> elements in footer"
      pattern: "facebook-placeholder"
---

<objective>
Fill the three remaining section stubs (#bo-suu-tap, #ve-kytoo, #lien-he) and update the footer with social links.

Purpose: Completes the landing page — after this plan, every section has real content. Visitors can see the lookbook gallery, read the brand story with social proof numbers, and find contact channels.
Output: Gallery grid (12 images, 2 featured col-span-2, no text), About 2-col layout (brand copy + stat numbers), Contact cards (Zalo + Facebook), Footer update (social icons + 2025 copyright).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-products-about-contact/02-CONTEXT.md
@.planning/phases/02-products-about-contact/02-RESEARCH.md
@.planning/phases/02-products-about-contact/02-PATTERNS.md
@.planning/phases/02-products-about-contact/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gallery grid in #bo-suu-tap</name>
  <files>index.html</files>
  <read_first>
    - index.html (full current state — includes Task 1 and Task 2 changes from plan 02-01)
    - CLAUDE.md Gallery Grid pattern: gap-1/gap-2, object-cover, no text overlay, bespoke masonry via col-span-2
    - 02-CONTEXT.md decision D-07 (visual-only gallery, reuse 12 images, CSS grid asymmetric)
    - 02-PATTERNS.md: Gallery Grid Pattern, Section Wrapper Pattern, Critical Warning about CSS native masonry
    - 02-RESEARCH.md: Pattern 5 (Gallery Grid), Anti-Patterns (no CSS columns, no native masonry), Pitfall 6
  </read_first>
  <action>
    Replace the single `<p class="text-kytoo-gray text-sm">— Phase 2 content —</p>` on the line inside `#bo-suu-tap` (currently line 106 in original file, shifted by Task 1 edits — locate by context: it is inside `<section id="bo-suu-tap" ...>`, after the heading div, before the closing `</div></section>` pair).

    Replace it with a CSS grid gallery. Grid container classes: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2`.

    The gallery has 12 cells arranged as follows — two cells use `col-span-2 aspect-video` (featured, wide), and the rest use `aspect-square` (regular). Assign featured slots to the two most visually impactful products: mysterybox (first featured) and tactical (second featured). Place them at positions that create visual rhythm:

    Cell order:
    1. mysterybox.jpg — `col-span-2 aspect-video` (featured, wide)
    2. blindbox.jpg — `aspect-square`
    3. wingman.jpg — `aspect-square`
    4. keycapcustom.jpg — `aspect-square`
    5. keycap14phim.jpg — `aspect-square`
    6. nhanclove.jpg — `aspect-square`
    7. nuochoa.jpg — `aspect-square`
    8. tactical.jpg — `col-span-2 aspect-video` (featured, wide)
    9. padchuot.jpg — `aspect-square`
    10. miengketay.jpg — `aspect-square`
    11. nhandaychuyen.png — `aspect-square`
    12. meotreomanhinh.jpg — `aspect-square`

    Each cell structure:
    - Featured cell: `<div class="col-span-2 aspect-video overflow-hidden"><img src="images/{file}" alt="" class="w-full h-full object-cover" aria-hidden="true"></div>`
    - Regular cell: `<div class="aspect-square overflow-hidden"><img src="images/{file}" alt="" class="w-full h-full object-cover" aria-hidden="true"></div>`

    All images: `alt=""` (empty) + `aria-hidden="true"` — purely decorative, no text overlays, no captions, no hover labels. This is the key visual differentiator from #san-pham.

    Do NOT use `columns-*`, do NOT use `grid-template-rows: masonry` or any inline style for masonry — use only `col-span-2` per 02-RESEARCH.md Anti-Patterns guidance.
    The section heading `<div class="flex items-center gap-2 mb-4">` block stays untouched.
  </action>
  <verify>
    <automated>grep -c "aria-hidden=\"true\"" index.html</automated>
    Expected: 13 (12 gallery images + 1 hero bg image already present from Phase 1)
    <automated>grep -c "col-span-2" index.html</automated>
    Expected: 2 (two featured gallery cells)
    <automated>grep -c "columns-" index.html</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - The `#bo-suu-tap` section stub `<p>` is replaced by a `<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">` containing exactly 12 image cells
    - Exactly 2 cells have `col-span-2 aspect-video` (mysterybox and tactical)
    - Exactly 10 cells have `aspect-square` (the remaining products)
    - All 12 `<img>` elements have `alt=""` and `aria-hidden="true"`
    - No text labels, price, or hover CTA anywhere inside `#bo-suu-tap` content
    - nhandaychuyen uses `.png` extension in img src
    - No `rounded-*` classes in any new HTML
    - Opening index.html in browser: gallery section shows a bespoke asymmetric image grid, no text on images
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: About content, Contact cards, and Footer social links</name>
  <files>index.html</files>
  <read_first>
    - index.html (current state after Task 1)
    - 02-CONTEXT.md decisions D-08 (about copy, placeholder stats), D-09 (contact Zalo/FB, footer)
    - 02-PATTERNS.md: About Section Layout Pattern, Contact Cards + Footer Pattern, Shared SVG Stroke Style, Critical Warning 5 (footer year 2024->2025), Critical Warning 6 (no Lucide for social icons)
    - 02-RESEARCH.md: Pattern 6 (About Layout), Pattern 7 (Contact + Footer), Pitfall 7 (footer year), Don't Hand-Roll (SVG inline for Zalo/FB)
  </read_first>
  <action>
    Three edits to index.html:

    **Edit A — #ve-kytoo about content:**
    Replace the stub `<p class="text-kytoo-gray text-sm">— Phase 2 content —</p>` inside `#ve-kytoo` with a 2-column layout (per D-08 and 02-PATTERNS.md About Section Layout Pattern):

    Container: `<div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">`

    Left column (brand copy, Claude draft):
    - First paragraph: "KYTOO la thuong hieu merch Valorant hang dau Viet Nam — noi nhung nguoi yeu game tim thay san pham the hien phong cach rieng cua minh. Moi thiet ke deu duoc tao ra boi fan, danh cho fan." (use full Vietnamese text from 02-RESEARCH.md Pattern 6 About Section Layout, or similar Claude draft anchored to Valorant-themed Vietnamese merch brand identity)
    - Second paragraph: "Tu keycap den mo hinh, trang suc den nuoc hoa — chung toi mang the gioi Valorant vao cuoc song hang ngay cua ban voi chat luong the hien dung tinh than game thu."
    - Both paragraphs: `class="text-kytoo-cream leading-relaxed mb-4"` and `class="text-kytoo-cream leading-relaxed"` respectively

    Right column (social proof stats, per D-08 — placeholder numbers):
    ```
    <div class="flex flex-col gap-6 md:items-end">
      Stat 1: number "1.000+" label "Don da giao" 
      Stat 2: number "4.8★" label "Danh gia tren Shopee"
    </div>
    ```
    Stat number element: `class="text-kytoo-red font-display font-bold text-5xl tracking-widest"`
    Stat label element: `class="text-kytoo-gray text-sm uppercase tracking-widest mt-1"`

    **Edit B — #lien-he contact cards:**
    Replace the stub `<p class="text-kytoo-gray text-sm">— Phase 2 content —</p>` inside `#lien-he` with two contact link cards side-by-side (per D-09 and 02-PATTERNS.md Contact Cards Pattern):

    Container: `<div class="flex flex-col sm:flex-row gap-4">`

    Zalo card: `<a href="#zalo-placeholder" class="flex items-center gap-3 border border-kytoo-border bg-kytoo-bg-alt px-6 py-4 hover:border-kytoo-red transition-colors">`
    Inside: SVG icon (24×24, Zalo letter "Z" style using two diagonal lines and a horizontal base — use a clean angular path with `stroke-linecap="square" stroke-width="2" fill="none" stroke="currentColor"`) + text div with `<p class="text-kytoo-cream font-semibold text-sm uppercase tracking-widest">Zalo</p>` and `<p class="text-kytoo-gray text-xs">Nhan tin truc tiep</p>`

    Facebook card: `<a href="#facebook-placeholder" class="flex items-center gap-3 border border-kytoo-border bg-kytoo-bg-alt px-6 py-4 hover:border-kytoo-red transition-colors">`
    Inside: SVG icon (24×24, Facebook "f" letterform using a standard path with `fill="currentColor"`) + text div with `<p class="text-kytoo-cream font-semibold text-sm uppercase tracking-widest">Facebook</p>` and `<p class="text-kytoo-gray text-xs">Like trang KYTOO</p>`

    SVG guidance for executor: Use the Zalo "Z" mark as two diagonal lines forming a Z shape with stroke-linecap="square". For Facebook, use the standard "f" path: `<path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>` (Lucide-style feathericon Facebook path, fill="currentColor"). Both SVGs must be inline — no Lucide CDN, no external icon source.

    No `rounded-*` on contact card `<a>` elements.

    **Edit C — Footer update:**
    Replace the entire `<footer>` element (currently lines 131–135 in original file, locate by `<footer class="bg-kytoo-bg border-t border-kytoo-border py-8">`) with the updated footer that:
    1. Keeps the same outer `<footer class="bg-kytoo-bg border-t border-kytoo-border py-8">` wrapper
    2. Keeps `<div class="max-w-7xl mx-auto px-4 text-center">`
    3. Adds a social icons row ABOVE the copyright: `<div class="flex items-center justify-center gap-6 mb-4">` containing two `<a>` links — one for Zalo (`href="#zalo-placeholder"`, `aria-label="Zalo"`) and one for Facebook (`href="#facebook-placeholder"`, `aria-label="Facebook"`). Each link: `class="text-kytoo-gray hover:text-kytoo-cream transition-colors"`. Each contains the same SVG icon used in the contact cards above, but scaled to 20×20.
    4. Updates copyright year: `<p class="text-kytoo-gray text-sm">© 2025 KYTOO Store. All rights reserved.</p>` — change 2024 to 2025.
  </action>
  <verify>
    <automated>grep -c "1.000+" index.html</automated>
    Expected: 1
    <automated>grep -c "4.8" index.html</automated>
    Expected: 1
    <automated>grep -c "zalo-placeholder" index.html</automated>
    Expected: 2 (one in #lien-he contact card, one in footer)
    <automated>grep -c "facebook-placeholder" index.html</automated>
    Expected: 2 (one in #lien-he contact card, one in footer)
    <automated>grep "2025 KYTOO" index.html</automated>
    Expected: copyright line with 2025 (not 2024)
    <automated>grep "2024" index.html</automated>
    Expected: 0 matches (old year removed)
    <automated>grep -c "rounded-" index.html</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - `#ve-kytoo` section: 2-column grid on md+ with brand copy (2 paragraphs) on left and social proof stats on right; "1.000+" and "4.8★" numbers rendered in `text-kytoo-red font-display font-bold text-5xl tracking-widest`
    - `#lien-he` section: two contact link cards (`<a>` tags) side-by-side (flex-row on sm+), each with a 24×24 SVG inline icon and descriptive text; href values are `#zalo-placeholder` and `#facebook-placeholder`
    - Footer: contains Zalo and Facebook `<a>` icon links above the copyright line; copyright reads "© 2025 KYTOO Store. All rights reserved."
    - "2024" does not appear anywhere in index.html
    - All SVG icons use inline SVG (no `<use>`, no Lucide CDN calls)
    - All SVG icons with stroke style use `stroke-linecap="square"` (Zalo icon)
    - No `rounded-*` class on contact cards or any new element
    - Opening index.html in browser: About section shows brand story on left and bold red stat numbers on right; Contact section shows two clickable cards with icons; footer shows social icons above copyright
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → static file | All content is static; no server, no user input, no data transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Contact placeholder links (#zalo-placeholder, #facebook-placeholder) | accept | Hash-only hrefs go nowhere; actual URLs added Phase 3; no user data collected |
| T-02-05 | Tampering | Footer social links (anchor tags) | accept | Placeholders only; real URLs vetted by user in Phase 3 before going live |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new external dependencies |
</threat_model>

<verification>
After both tasks complete, open index.html in browser and verify:
1. Gallery section shows asymmetric image grid — 2 wide featured images (mysterybox, tactical) and 10 square cells; no text on any image
2. About section shows brand copy on left, large red "1.000+" and "4.8★" stat numbers on right (2-col on desktop, stacked on mobile)
3. Contact section shows Zalo and Facebook cards with icons; both cards have hover border-red effect
4. Footer shows Zalo + Facebook icon links above "© 2025 KYTOO Store. All rights reserved."
5. Full page scroll: all 4 sections have content, no "Phase 2 content" stubs remain anywhere
6. Zero rounded-* classes (grep confirms)
7. Zero occurrences of "2024" in the file (grep confirms)
</verification>

<success_criteria>
- All four section stubs replaced with real content (zero "Phase 2 content" strings remaining in index.html)
- Gallery: 12 images, 2 featured col-span-2, no text overlays, aria-hidden on all gallery images
- About: brand copy + "1.000+" and "4.8★" stats visible in kytoo-red
- Contact: Zalo and Facebook cards with placeholder hrefs (#zalo-placeholder, #facebook-placeholder)
- Footer: social icon links present, copyright updated to 2025
- Zero rounded-* classes across entire file
</success_criteria>

<output>
Create `.planning/phases/02-products-about-contact/02-02-SUMMARY.md` when done
</output>
