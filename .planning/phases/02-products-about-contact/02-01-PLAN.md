---
phase: 02-products-about-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03

must_haves:
  truths:
    - "Opening index.html shows a 12-item product grid under the #san-pham heading"
    - "Each card displays a product image (aspect-square, object-cover), the full product name (line-clamp-2), and the price in kytoo-red"
    - "Clicking any product card opens a modal with a large image, full name, price, 1-2 sentence description, and a Mua tren Shopee button"
    - "The Shopee button in the modal links to #shopee-{productKey} and opens in a new tab"
    - "The modal closes via the X button, clicking the dark overlay, or pressing Escape"
    - "Body scrolling is locked while the modal is open and restored on close"
    - "No rounded-* class appears anywhere in the new HTML"
  artifacts:
    - path: "index.html"
      provides: "Product grid (12 cards) replacing line 96 stub; modal overlay element before </body>; PRODUCTS data object + openModal/closeModal/handleModalOverlayClick/keydown functions in existing <script> block"
      contains: "id=\"san-pham\"  id=\"product-modal\"  openModal  closeModal  PRODUCTS"
  key_links:
    - from: "product card onclick"
      to: "openModal(productKey)"
      via: "onclick attribute on <div>"
      pattern: "onclick=\"openModal\\("
    - from: "openModal()"
      to: "PRODUCTS object"
      via: "PRODUCTS[productId]"
      pattern: "PRODUCTS\\[productId\\]"
    - from: "modal-shopee-link"
      to: "#shopee-{productKey}"
      via: "document.getElementById('modal-shopee-link').href = p.shopee"
      pattern: "shopee-"
---

<objective>
Replace the #san-pham stub with a real 12-product grid and wire the modal popup.

Purpose: Visitors can browse all 12 products, click any card to see details, and reach the Shopee listing. This is the core commercial content of the landing page.
Output: 12 product cards in a 2→4 column responsive grid; a product-modal overlay element; a PRODUCTS data object with all 12 entries; openModal/closeModal/handleModalOverlayClick functions and an Escape keydown listener appended to the existing script block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-products-about-contact/02-CONTEXT.md
@.planning/phases/02-products-about-contact/02-RESEARCH.md
@.planning/phases/02-products-about-contact/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 12-product card grid in #san-pham</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — 159 lines; already in context above)
    - CLAUDE.md design system rules: no rounded-*, kytoo-* tokens only, card hover pattern
    - 02-CONTEXT.md decisions D-01 through D-04 (grid cols, full name, sort order, click behavior)
    - 02-PATTERNS.md: Product Card Pattern, Product Grid Pattern, Section Wrapper Pattern
  </read_first>
  <action>
    Replace line 96 in index.html — the single `<p class="text-kytoo-gray text-sm">— Phase 2 content —</p>` inside `#san-pham` — with a 12-card product grid. Keep the section outer shell (lines 90–95 and 97–98) completely untouched.

    The replacement is a single `<div>` grid wrapping 12 `<article>` elements. Grid classes (per D-01 and 02-PATTERNS.md Product Grid Pattern): `grid grid-cols-2 lg:grid-cols-4 gap-4`.

    Each `<article>` follows the Product Card Pattern from 02-PATTERNS.md exactly:
    - Outer: `<article class="bg-kytoo-bg-alt border border-kytoo-border cursor-pointer transition-all duration-200 hover:border-kytoo-red hover:scale-105" onclick="openModal('{key}')">`
    - Image wrapper: `<div class="aspect-square overflow-hidden"><img src="images/{file}" alt="{short alt}" class="w-full h-full object-cover"></div>`
    - Text area: `<div class="p-3"><p class="text-kytoo-cream text-xs leading-snug mb-1 line-clamp-2">{full name}</p><p class="text-kytoo-red text-sm font-semibold">{price}</p></div>`
    - NO `rounded-*` anywhere, NO `<a>` wrapper, NO "Mua tren Shopee" button on the card itself (per D-04)

    Product order is price-descending per D-03. Use the exact keys, filenames, full names, and prices from 02-RESEARCH.md Complete Product Data Table (12 rows). Critical: `nhandaychuyen` uses `images/nhandaychuyen.png` (PNG, not JPG). All others use `.jpg`.

    The 12 product entries in order:
    1. key=mysterybox, file=mysterybox.jpg, price=150.000₫ – 279.000₫
    2. key=keycapcustom, file=keycapcustom.jpg, price=279.000₫
    3. key=keycap14phim, file=keycap14phim.jpg, price=251.100₫ – 279.000₫
    4. key=tactical, file=tactical.jpg, price=279.000₫
    5. key=wingman, file=wingman.jpg, price=152.100₫
    6. key=nhanclove, file=nhanclove.jpg, price=79.000₫
    7. key=nuochoa, file=nuochoa.jpg, price=69.000₫
    8. key=blindbox, file=blindbox.jpg, price=67.150₫
    9. key=padchuot, file=padchuot.jpg, price=63.200₫ – 79.000₫
    10. key=miengketay, file=miengketay.jpg, price=55.200₫
    11. key=nhandaychuyen, file=nhandaychuyen.png, price=37.000₫ – 44.100₫
    12. key=meotreomanhinh, file=meotreomanhinh.jpg, price=35.000₫

    Use the verbatim full product names from 02-RESEARCH.md Complete Product Data Table. Price ranges use em dash (–) and ₫ symbol, matching that table exactly.
  </action>
  <verify>
    <automated>grep -c "onclick=\"openModal(" index.html</automated>
    Expected: 12 (one per card)
    <automated>grep -c "article" index.html</automated>
    Expected: 24 (12 opening + 12 closing tags)
    <automated>grep -c "rounded-" index.html</automated>
    Expected: 0
    <automated>grep "nhandaychuyen" index.html</automated>
    Expected: line contains `images/nhandaychuyen.png` (not .jpg)
  </verify>
  <acceptance_criteria>
    - index.html line 96 (the stub `<p>`) is gone; replaced by a `<div class="grid grid-cols-2 lg:grid-cols-4 gap-4">` containing exactly 12 `<article>` elements
    - Each article has `onclick="openModal('{key}')"` with the correct key matching the product
    - Image src for nhandaychuyen is `images/nhandaychuyen.png` (not .jpg)
    - Product names on cards use `line-clamp-2` and match verbatim names from 02-RESEARCH.md
    - Prices render in `text-kytoo-red` and match the price column from 02-RESEARCH.md
    - Zero `rounded-*` class occurrences anywhere in the file (verified by grep)
    - The section heading `<div class="flex items-center gap-2 mb-4">` block is unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add product modal HTML element and wire JS functions</name>
  <files>index.html</files>
  <read_first>
    - index.html (current state after Task 1)
    - 02-CONTEXT.md decisions D-05, D-06 (modal content, modal close behavior)
    - 02-PATTERNS.md: Modal Overlay Pattern, JS Script Block insertion point, Critical Implementation Warnings
    - 02-RESEARCH.md: Pattern 3 (Modal HTML), Pattern 4 (Modal JS), Pitfalls 2, 3, 4
  </read_first>
  <action>
    Two edits to index.html:

    **Edit A — Modal HTML element:**
    Insert the modal element immediately before `</body>` (after the closing `</script>` tag, currently at line 157). The modal element is a single `<div id="product-modal">` with `hidden` class by default.

    Modal outer div: `id="product-modal"`, classes `hidden fixed inset-0 z-[100] flex items-center justify-center bg-kytoo-bg/80 backdrop-blur-sm`, attribute `onclick="handleModalOverlayClick(event)"`.

    Modal inner card (child of outer div): classes `bg-kytoo-bg-alt border border-kytoo-border w-full max-w-lg mx-4 relative`, attribute `onclick="event.stopPropagation()"`. NO `rounded-*` on this div.

    Inside the inner card:
    - Close button: `<button onclick="closeModal()" class="absolute top-3 right-3 text-kytoo-gray hover:text-kytoo-cream transition-colors" aria-label="Dong">` containing a 20×20 SVG X icon with `stroke-linecap="square"` (exact SVG from 02-PATTERNS.md Modal Overlay Pattern)
    - Image wrapper: `<div class="aspect-square overflow-hidden"><img id="modal-img" src="" alt="" class="w-full h-full object-cover"></div>`
    - Content div: `<div class="p-6">` containing:
      - `<p id="modal-name" class="text-kytoo-cream font-semibold text-base mb-1 leading-snug"></p>`
      - `<p id="modal-price" class="text-kytoo-red font-bold text-lg mb-3"></p>`
      - `<p id="modal-desc" class="text-kytoo-gray text-sm leading-relaxed mb-5"></p>`
      - Shopee link: `<a id="modal-shopee-link" href="#shopee-placeholder" target="_blank" rel="noopener" class="block text-center bg-kytoo-red text-white uppercase tracking-widest px-8 py-3 text-sm font-semibold hover:bg-kytoo-red-dark transition-colors">Mua tren Shopee</a>`

    z-index must be `z-[100]` — the navbar is `z-50`; modal must render above it per 02-PATTERNS.md Critical Warning 3.

    **Edit B — JS appended to existing script block:**
    Inside the existing `<script>` block (lines 137–157), append after line 156 (the closing `}` of the existing click-outside handler) and before line 157 (`</script>`):

    1. `const PRODUCTS = { ... }` — 12 product entries using keys from Task 1. Each entry has: `name` (verbatim full name from 02-RESEARCH.md), `price` (verbatim), `img` (e.g., `images/mysterybox.jpg`), `desc` (1–2 sentence Claude draft from 02-RESEARCH.md Complete Product Data Table), `shopee` (e.g., `#shopee-mysterybox`). Use `.png` for nhandaychuyen.

    2. `function openModal(productId)` — reads `PRODUCTS[productId]`, sets `modal-img.src`, `modal-img.alt`, `modal-name.textContent`, `modal-price.textContent`, `modal-desc.textContent`, `modal-shopee-link.href`, removes `hidden` from `product-modal`, sets `document.body.style.overflow = 'hidden'`.

    3. `function closeModal()` — adds `hidden` to `product-modal`, resets `document.body.style.overflow = ''`.

    4. `function handleModalOverlayClick(event)` — calls `closeModal()` only if `event.target === document.getElementById('product-modal')`.

    5. `document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeModal(); });`

    Do NOT create a new `<script>` tag — append all JS inside the existing block.

    The modal descriptions to use (from 02-RESEARCH.md):
    - mysterybox: "Mo hop bi an de nhan bo skin Valorant ngau nhien — them phu kien gia do va card dac biet tang kem."
    - keycapcustom: "Keycap 3D in hinh Agent Valorant noi bat, chat lieu nhua tong hop ben — nang cap ban phim co cua ban ngay."
    - keycap14phim: "Bo 14 keycap PBT chuan OEM in hinh Agent de thuong — tuong thich voi hau het ban phim co va khong day."
    - tactical: "Mo hinh Tactical Bear chinh xac tung chi tiet — flex ban gaming, xa stress cuc hieu qua."
    - wingman: "Nguoi ban dong hanh Wingman thu nho — trang tri ban lam viec va flex voi dong doi cung chung server."
    - nhanclove: "Bo 3 nhan lay cam hung tu Agent Clove — size tuy chinh, ly tuong cho cap doi game thu."
    - nuochoa: "Nuoc hoa mini 10ml huong thien nhien — mang ca tinh Agent Valorant vao cuoc song thuc cua ban."
    - blindbox: "Moc khoa skin Valorant bi an — moi tui mu la mot skin ngau nhien, kem sticker tang kem."
    - padchuot: "Pad chuot handmade 30x25cm in hoa van Valorant — bao ve mat ban va tang do chinh xac khi aim."
    - miengketay: "Ke tay hinh Corgi de thuong — giam moi co tay sau nhung session gaming dai."
    - nhandaychuyen: "Nhan lay cam hung tu Agent Sage — don gian, tinh te, danh cho nguoi yeu Valorant."
    - meotreomanhinh: "Meo nho treo mep man hinh — trang tri ban lam viec them cute ma khong ton khong gian."

    Use the Vietnamese description text from 02-RESEARCH.md Complete Product Data Table (the exact Vietnamese sentences in the `desc` field column).
  </action>
  <verify>
    <automated>grep -c "id=\"product-modal\"" index.html</automated>
    Expected: 1
    <automated>grep -c "function openModal" index.html</automated>
    Expected: 1
    <automated>grep -c "function closeModal" index.html</automated>
    Expected: 1
    <automated>grep -c "Escape" index.html</automated>
    Expected: 1
    <automated>grep -c "<script" index.html</automated>
    Expected: 2 (the Tailwind config script in head + the existing JS block — no new script tag added)
    <automated>grep "z-\[100\]" index.html</automated>
    Expected: the modal outer div line contains z-[100]
    <automated>grep "nhandaychuyen" index.html</automated>
    Expected: two occurrences, both using .png extension (one in the card img from Task 1, one in PRODUCTS JS object)
  </verify>
  <acceptance_criteria>
    - index.html contains exactly one element with `id="product-modal"` with `class` including `hidden` and `z-[100]`
    - Modal inner card div has NO `rounded-*` class
    - The modal close button has `aria-label="Dong"` (or "Dong" in Vietnamese)
    - Modal Shopee link has `target="_blank" rel="noopener"` attributes
    - PRODUCTS object contains exactly 12 keys: mysterybox, keycapcustom, keycap14phim, tactical, wingman, nhanclove, nuochoa, blindbox, padchuot, miengketay, nhandaychuyen, meotreomanhinh
    - nhandaychuyen entry in PRODUCTS has `img: 'images/nhandaychuyen.png'`
    - closeModal() is the sole function that manipulates `product-modal hidden` class and resets body overflow — called by X button onclick, handleModalOverlayClick, and the keydown Escape listener
    - No new `<script>` tag was added; `grep -c "<script" index.html` returns 2
    - Opening index.html in browser: clicking any product card opens the modal with the correct product image, name, price, and description populated
    - Pressing Escape closes the modal and page scrolling is restored
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → static file | index.html is served as a local file or static host; no server-side processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Shopee placeholder links (`#shopee-*`) | accept | Placeholder anchors go nowhere; no user data transmitted; real URLs added Phase 3 |
| T-02-02 | Information Disclosure | `target="_blank"` on Shopee link | mitigate | `rel="noopener"` already specified in pattern — prevents opener access from new tab |
| T-02-03 | Denial of Service | Body scroll lock not released | mitigate | All three close paths (X, overlay, Escape) call the same `closeModal()` function which always resets `document.body.style.overflow = ''` |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new external dependencies |
</threat_model>

<verification>
After both tasks complete, verify in browser:
1. Page shows 12 product cards in a 2-col (mobile) / 4-col (desktop) grid under the "San Pham" heading
2. Click any card — modal opens with correct image, name, price, description
3. Click "Mua tren Shopee" button — opens new tab (even to a #hash placeholder)
4. Click the dark overlay outside the modal card — modal closes, scroll restores
5. Click X button — modal closes, scroll restores
6. Press Escape — modal closes, scroll restores
7. No broken images (all 12 load, including nhandaychuyen.png)
</verification>

<success_criteria>
- 12 product cards visible in responsive grid (2 cols mobile, 4 cols desktop) with images, names, prices
- Click any card -> modal opens with product-specific content
- Three modal close paths all work (X, overlay, Escape)
- Zero rounded-* classes in index.html
- grep -c "<script" index.html returns 2 (no extra script tags added)
- nhandaychuyen.png loads without 404
</success_criteria>

<output>
Create `.planning/phases/02-products-about-contact/02-01-SUMMARY.md` when done
</output>
