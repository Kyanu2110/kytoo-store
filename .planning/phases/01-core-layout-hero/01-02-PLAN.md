---
phase: 01-core-layout-hero
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04

must_haves:
  truths:
    - "Opening index.html in browser shows a fullscreen hero section filled by VALORANT_Jett_Red_1_1.webp with a dark gradient overlay — no white gap, no uncovered viewport"
    - "Large 'KYTOO' heading in Rajdhani Bold appears centered on the hero, legible against the background"
    - "A short tagline placeholder line appears below the heading in Inter, centered"
    - "A red CTA button 'Khám phá sản phẩm' appears below the tagline; clicking it smooth-scrolls down to the #san-pham section"
    - "The navbar remains visible and correctly overlays the top of the hero section"
    - "On 320px mobile viewport the hero text and CTA are fully visible and not clipped"
  artifacts:
    - path: "index.html"
      provides: "Fully implemented hero section replacing the empty placeholder from Plan 01-01"
      contains: "VALORANT_Jett_Red_1_1.webp"
  key_links:
    - from: "hero CTA button"
      to: "#san-pham section"
      via: "href attribute + CSS scroll-behavior smooth"
      pattern: "href=\"#san-pham\""
    - from: "hero background image"
      to: "VALORANT_Jett_Red_1_1.webp"
      via: "CSS background-image or inline style"
      pattern: "VALORANT_Jett_Red_1_1.webp"
    - from: "gradient overlay div"
      to: "hero readability"
      via: "absolute positioned div with Tailwind gradient"
      pattern: "bg-gradient-to-t from-kytoo-bg"
---

<objective>
Implement the full hero section inside index.html: fullscreen background image with dark gradient overlay, large "KYTOO" display heading, tagline, and a CTA button that smooth-scrolls to the products section.

Purpose: The hero is the first impression — it communicates the Valorant-gaming brand identity instantly. This plan converts the empty hero placeholder from Plan 01-01 into a polished, branded first screen.
Output: index.html hero section (id="hero") complete — VALORANT_Jett_Red_1_1.webp background, dark overlay, Rajdhani Bold heading, Inter tagline, red CTA button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-layout-hero/01-CONTEXT.md
@CLAUDE.md
@.planning/phases/01-core-layout-hero/01-01-SUMMARY.md

<interfaces>
<!-- Key structure from Plan 01-01 that this plan modifies. -->

From index.html (created by Plan 01-01):
<!-- Hero placeholder to REPLACE: -->
<!--   <section id="hero" class="min-h-screen bg-kytoo-bg"></section> -->

<!-- Tailwind custom tokens available (declared in tailwind.config): -->
<!--   kytoo-bg: #0F1923 -->
<!--   kytoo-bg-alt: #1a2634 -->
<!--   kytoo-red: #FF4655 -->
<!--   kytoo-red-dark: #BD3944 -->
<!--   kytoo-cream: #ECE8E1 -->
<!--   kytoo-gray: #768079 -->
<!--   kytoo-border: #2a3f52 -->
<!--   fontFamily.display: ['Rajdhani', 'sans-serif'] -->
<!--   fontFamily.body: ['Inter', 'sans-serif'] -->
<!--   scrollMarginTop.navbar: '4rem' -->

<!-- Section IDs available for CTA scroll target: -->
<!--   id="san-pham"  — with class scroll-mt-navbar -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero placeholder with fullscreen background image and gradient overlay</name>

  <read_first>
    - CLAUDE.md — Hero Section pattern: min-h-screen, background-image with overlay "bg-gradient-to-t from-[#0F1923] via-transparent", object-cover, image treatment
    - .planning/phases/01-core-layout-hero/01-CONTEXT.md — decision D-09 (exact overlay gradient: bg-gradient-to-t from-kytoo-bg via-kytoo-bg/60 to-transparent), decision D-07 (centered alignment)
    - .planning/phases/01-core-layout-hero/01-01-SUMMARY.md — confirm exact class names and structure of the hero placeholder to replace
  </read_first>

  <files>index.html</files>

  <action>
    Replace the empty section id="hero" placeholder from Plan 01-01 with the full hero implementation (per D-09, HERO-01):

    Outer section: id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden"

    Layer 1 — Background image (absolute fill):
    - img element: src="VALORANT_Jett_Red_1_1.webp" alt="" class="absolute inset-0 w-full h-full object-cover object-center" aria-hidden="true"

    Layer 2 — Gradient overlay (absolute fill, on top of image, per D-09):
    - div class="absolute inset-0 bg-gradient-to-t from-kytoo-bg via-kytoo-bg/60 to-transparent"

    Layer 3 — Content (relative, on top of overlay):
    - div class="relative z-10 text-center px-4 max-w-4xl mx-auto"
    - This div holds the heading, tagline, and CTA (added in Task 2)

    The section needs pt-16 at minimum to avoid content hiding behind the fixed navbar, but since the section is flex items-center with min-h-screen, the content will center vertically. The navbar overlays transparently — no additional top padding needed on the section itself since the content is centered in the full viewport height.

    After replacing, the hero section must be positioned so the fixed nav (z-50) overlays it correctly — the hero section itself has no z-index, just relative positioning.
  </action>

  <verify>
    <automated>grep -c "VALORANT_Jett_Red_1_1.webp" "D:/Dự Án Claude/Web Kytoo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - section id="hero" has class containing: relative min-h-screen flex items-center justify-center overflow-hidden
    - img element inside hero has src="VALORANT_Jett_Red_1_1.webp" and class containing: absolute inset-0 w-full h-full object-cover
    - Gradient overlay div exists inside hero with class containing: absolute inset-0 bg-gradient-to-t from-kytoo-bg
    - Gradient overlay includes via-kytoo-bg/60 (partial midpoint opacity per D-09)
    - Content wrapper div exists with class containing: relative z-10 text-center
    - Opening index.html in browser at 1024px shows hero image filling full viewport height
    - Opening at 320px shows hero image filling full viewport height without horizontal scroll
    - Dark gradient is visible at bottom of hero image — image does not abruptly cut off
  </acceptance_criteria>

  <done>Hero background image and gradient overlay render correctly at all breakpoints — VALORANT_Jett_Red_1_1.webp fills the viewport with a dark-to-transparent gradient overlay</done>
</task>

<task type="auto">
  <name>Task 2: Add KYTOO heading, tagline, and smooth-scroll CTA button to hero content</name>

  <read_first>
    - CLAUDE.md — Typography: display headings uppercase bold font-weight 800-900 tracking-widest; Button pattern: bg-[#FF4655] uppercase tracking-widest px-8 py-3 NO border-radius; Body text: #ECE8E1 leading-relaxed
    - .planning/phases/01-core-layout-hero/01-CONTEXT.md — decisions D-05 (heading "KYTOO" Rajdhani Bold), D-06 (tagline placeholder), D-07 (centered), D-08 (CTA "Khám phá sản phẩm" smooth-scrolls to #san-pham), HERO-02, HERO-03, HERO-04
  </read_first>

  <files>index.html</files>

  <action>
    Inside the hero content wrapper div (class="relative z-10 text-center px-4 max-w-4xl mx-auto") created in Task 1, add (per D-05, D-06, D-07, D-08):

    Small brand label above heading (CLAUDE.md label pattern: uppercase small caps, text-xs tracking-widest, red):
    - p class="text-kytoo-red text-xs uppercase tracking-widest mb-4 font-body font-semibold" with text "Merch Valorant Việt Nam"

    Main display heading (per D-05, HERO-02):
    - h1 class="font-display font-extrabold text-7xl md:text-9xl uppercase tracking-widest text-kytoo-cream leading-none mb-6" with text "KYTOO"
    - Note: font-extrabold applies font-weight 800 — use font-display (Rajdhani) for the condensed military aesthetic per D-10

    Tagline (per D-06, HERO-03):
    - p class="text-kytoo-cream text-lg md:text-xl font-body leading-relaxed mb-8 max-w-xl mx-auto" with text "Merch gaming chính hiệu — thiết kế độc quyền cho fan Valorant"
    - This is the placeholder text per D-06; user will update after seeing layout

    CTA button (per D-08, HERO-04):
    - a element with href="#san-pham" (smooth scroll to products section) and class="inline-block bg-kytoo-red text-white uppercase tracking-widest px-10 py-4 font-body font-semibold text-sm hover:bg-kytoo-red-dark transition-colors" with text "Khám phá sản phẩm"
    - NO rounded-* class on this element — strict per CLAUDE.md and D-03 button pattern
    - href="#san-pham" links to the section with scroll-mt-navbar class added in Plan 01-01

    Smooth scroll behavior: add "scroll-behavior: smooth" to the html element via adding class "scroll-smooth" to the html tag (Tailwind utility). This enables CSS smooth scrolling for all anchor links on the page.
  </action>

  <verify>
    <automated>grep -c "Khám phá sản phẩm" "D:/Dự Án Claude/Web Kytoo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - h1 element inside hero content wrapper has class containing: font-display font-extrabold uppercase tracking-widest text-kytoo-cream
    - h1 text content is exactly "KYTOO"
    - h1 has text-7xl on mobile and md:text-9xl on desktop (responsive sizing)
    - Tagline p element has class containing: font-body text-kytoo-cream leading-relaxed
    - Tagline text is a non-empty placeholder string (not empty, not TODO)
    - CTA anchor href="#san-pham"
    - CTA anchor text is "Khám phá sản phẩm"
    - CTA anchor class contains: bg-kytoo-red text-white uppercase tracking-widest
    - CTA anchor has NO rounded-* class (no border-radius)
    - CTA anchor has hover:bg-kytoo-red-dark class for hover state
    - html element has class scroll-smooth (enables CSS smooth scrolling)
    - Small brand label exists above h1 with text containing "Merch Valorant" and class containing text-kytoo-red uppercase tracking-widest
    - Opening index.html in browser: "KYTOO" heading visible in large bold condensed font overlaying the hero image
    - Clicking "Khám phá sản phẩm" button smooth-scrolls to #san-pham section without page reload
    - On 320px viewport: full "KYTOO" heading (may wrap or scale) and CTA button visible without horizontal scroll
  </acceptance_criteria>

  <done>Hero section complete — "KYTOO" heading in Rajdhani Bold, tagline placeholder, and "Khám phá sản phẩm" CTA button all render centered on the hero image. CTA smooth-scrolls to products section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → browser | VALORANT_Jett_Red_1_1.webp loaded as relative path from same directory |
| Hero CTA → internal section | Anchor link navigates within the same page — no external request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Hero image path | accept | Image is a local asset; path is not sensitive; static site with no server-side exposure |
| T-02-02 | Tampering | Hero CTA href="#san-pham" | accept | Internal anchor link — no user input, no redirect attack surface |
| T-02-03 | Denial of Service | Large .webp image blocking paint | mitigate | img has explicit width/height (w-full h-full) preventing layout shift; browser loads inline before paint |
| T-02-SC | Tampering | No npm/pip/cargo installs in this plan | accept | Hero implementation is pure HTML/CSS — no package manager involved |
</threat_model>

<verification>
Run these checks after plan execution completes:

1. Hero image reference: grep "VALORANT_Jett_Red_1_1.webp" index.html (1+ match)
2. Gradient overlay: grep "bg-gradient-to-t from-kytoo-bg" index.html (1 match)
3. Heading text: grep "KYTOO" index.html — appears inside h1 (not just config)
4. CTA text: grep "Khám phá sản phẩm" index.html (1 match)
5. CTA href: grep "href=\"#san-pham\"" index.html (at least 1 match — the hero CTA)
6. Scroll smooth: grep "scroll-smooth" index.html (on html element)
7. No border-radius on CTA: verify CTA anchor has no rounded-* class
8. Browser test at 1024px: fullscreen hero image visible, "KYTOO" in large Rajdhani Bold, gradient overlay darkening bottom, CTA button in red
9. Browser test at 320px: hero visible, no horizontal scroll, CTA button fully visible
10. Browser interaction: click "Khám phá sản phẩm" → page smooth-scrolls to "Sản Phẩm" section heading
</verification>

<success_criteria>
- VALORANT_Jett_Red_1_1.webp fills full viewport height (min-h-screen) with object-cover
- Dark gradient overlay (from-kytoo-bg via-kytoo-bg/60 to-transparent) renders across the image
- "KYTOO" in Rajdhani Bold at text-7xl (mobile) / text-9xl (desktop), centered
- Tagline placeholder text centered below heading
- Red CTA button "Khám phá sản phẩm" smooth-scrolls to #san-pham section
- Navbar from Plan 01-01 overlays hero correctly (z-50 above hero)
- Layout intact at 320px — no horizontal scroll, no content clipping
</success_criteria>

<output>
Create `.planning/phases/01-core-layout-hero/01-02-SUMMARY.md` when done
</output>
