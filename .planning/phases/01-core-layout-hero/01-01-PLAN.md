---
phase: 01-core-layout-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - TECH-01
  - TECH-02

must_haves:
  truths:
    - "Opening index.html in browser shows a dark-themed page with a fixed navbar — no white flash, no unstyled content"
    - "Navbar displays centered KYTOO logo with nav links symmetric on both sides"
    - "On < 768px, hamburger icon appears; tapping it opens a slide-down drawer with all 4 nav links"
    - "Page structure contains 4 empty sections with correct IDs (#san-pham, #bo-suu-tap, #ve-kytoo, #lien-he) and a footer"
    - "Custom Tailwind colors (#0F1923, #FF4655, #ECE8E1, #1a2634, #2a3f52, #768079, #BD3944) are available globally"
    - "Rajdhani Bold and Inter fonts load from Google Fonts CDN"
  artifacts:
    - path: "index.html"
      provides: "Full HTML skeleton with Tailwind CDN, Google Fonts, navbar, 4 section placeholders, footer"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "navbar anchor links"
      to: "#san-pham, #bo-suu-tap, #ve-kytoo, #lien-he section IDs"
      via: "href attribute matching section id attributes"
      pattern: "href=\"#san-pham\""
    - from: "tailwind.config"
      to: "custom color tokens"
      via: "theme.extend.colors"
      pattern: "\"kytoo-red\": \"#FF4655\""
---

<objective>
Bootstrap index.html as the single-file project foundation: Tailwind CDN configured with custom design tokens, Google Fonts loaded, fixed navbar with symmetric layout and mobile hamburger menu, 4 empty section placeholders, and a minimal footer.

Purpose: Every subsequent plan (hero, products, about) builds directly on this skeleton. Phase 2 fills the section placeholders without touching the structural scaffold.
Output: A single working index.html that opens in any browser with correct colors, fonts, sticky navbar, and empty sections — no white flash, no broken layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-layout-hero/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with Tailwind setup, Google Fonts, and custom color tokens</name>

  <read_first>
    - CLAUDE.md — full design system: exact hex codes, typography rules, button/card patterns
    - .planning/phases/01-core-layout-hero/01-CONTEXT.md — decisions D-10, D-11, D-12 for font choices; design token names
  </read_first>

  <files>index.html</files>

  <action>
    Create index.html from scratch with the following structure:

    HEAD setup (decisions D-10, D-11, D-12, TECH-02):
    - DOCTYPE html, lang="vi", charset=UTF-8, viewport meta (width=device-width, initial-scale=1.0)
    - Google Fonts import: single preconnect + stylesheet link loading "Rajdhani:wght@700;800" and "Inter:wght@400;500;600" — use the combined families URL format
    - Tailwind CDN: script tag with src="https://cdn.tailwindcss.com"
    - Immediately after the CDN script: a script tag with type="text/tailwindcss" containing tailwind.config:
      - theme.extend.colors: kytoo-bg (#0F1923), kytoo-bg-alt (#1a2634), kytoo-red (#FF4655), kytoo-red-dark (#BD3944), kytoo-cream (#ECE8E1), kytoo-gray (#768079), kytoo-border (#2a3f52)
      - theme.extend.fontFamily: display (['Rajdhani', 'sans-serif']), body (['Inter', 'sans-serif'])
    - title: "KYTOO Store — Merch Valorant Việt Nam"
    - Body: class="bg-kytoo-bg text-kytoo-cream font-body antialiased"

    This task creates only the document shell — no navbar or sections yet. The file must be a valid HTML document that renders a dark background (#0F1923) when opened.
  </action>

  <verify>
    <automated>grep -c "cdn.tailwindcss.com" "D:/Dự Án Claude/Web Kytoo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - index.html exists at project root with valid DOCTYPE html declaration
    - head contains: charset=UTF-8, viewport meta, Google Fonts link for Rajdhani (700,800) and Inter (400,500,600)
    - head contains: script src="https://cdn.tailwindcss.com"
    - head contains: script type="text/tailwindcss" with tailwind.config theme.extend.colors block defining kytoo-bg, kytoo-red, kytoo-cream, kytoo-bg-alt, kytoo-red-dark, kytoo-gray, kytoo-border
    - head contains: script type="text/tailwindcss" with fontFamily.display = ['Rajdhani', 'sans-serif'] and fontFamily.body = ['Inter', 'sans-serif']
    - body element has class containing bg-kytoo-bg and font-body
    - Opening index.html in browser renders dark background — no white flash
  </acceptance_criteria>

  <done>index.html is a valid HTML document with Tailwind CDN, custom color tokens, and Google Fonts — browser renders #0F1923 background</done>
</task>

<task type="auto">
  <name>Task 2: Fixed navbar with symmetric layout, nav links, CTA button, and mobile hamburger</name>

  <read_first>
    - CLAUDE.md — Navbar pattern: fixed top, h-16, bg #0F1923, border-b #2a3f52; Button pattern: bg-[#FF4655] uppercase tracking-widest px-8 py-3 NO border-radius
    - .planning/phases/01-core-layout-hero/01-CONTEXT.md — decisions D-01 (centered logo), D-02 (4 nav links), D-03 (CTA "Mua ngay" → Shopee), D-04 (mobile hamburger slide-down drawer)
  </read_first>

  <files>index.html</files>

  <action>
    Add a nav element inside body, before any section content (per D-01, D-02, D-03, D-04):

    Navbar structure — nav with class "fixed top-0 left-0 right-0 z-50 h-16 bg-kytoo-bg border-b border-kytoo-border":
    - Inner div: "max-w-7xl mx-auto px-4 h-full flex items-center justify-between relative"
    - LEFT group (desktop only, hidden on mobile): div class="hidden md:flex items-center gap-8" containing anchor links to #san-pham and #bo-suu-tap. Each link: class="text-kytoo-cream text-sm uppercase tracking-widest hover:text-kytoo-red transition-colors"
    - CENTER: div class="absolute left-1/2 -translate-x-1/2" containing a span or div with the text "KYTOO" in class="font-display font-bold text-xl tracking-widest text-kytoo-cream uppercase"
    - RIGHT group (desktop): div class="hidden md:flex items-center gap-8" containing: anchor link to #ve-kytoo, anchor link to #lien-he (same styling as LEFT links), then the CTA anchor "Mua ngay" with href="#shopee-placeholder" (per D-03 — Shopee URL will be filled Phase 3) and class="bg-kytoo-red text-white uppercase tracking-widest px-6 py-2 text-sm font-semibold hover:bg-kytoo-red-dark transition-colors" — NO rounded-* class anywhere on this button
    - HAMBURGER (mobile only): button element with id="hamburger-btn" class="md:hidden text-kytoo-cream p-2" containing an SVG inline hamburger icon (3 horizontal lines, each a rect or path, 24x24 viewBox)

    Mobile drawer (per D-04):
    - div with id="mobile-menu" class="md:hidden hidden bg-kytoo-bg border-b border-kytoo-border" — positioned immediately after the nav's inner div but still inside the nav element
    - Inside mobile-menu: div class="max-w-7xl mx-auto px-4 py-4 flex flex-col gap-4" with all 4 anchor links (same as desktop: #san-pham, #bo-suu-tap, #ve-kytoo, #lien-he) plus the "Mua ngay" CTA link — links styled same as desktop nav links

    Vanilla JS (per D-04, Claude's Discretion for scroll/toggle behavior):
    - Add a script tag at the end of body (create the closing script block that will accumulate all JS for the phase)
    - JS: hamburger-btn click toggles mobile-menu's "hidden" class
    - JS: each anchor inside mobile-menu, on click, adds "hidden" class back to mobile-menu (closes drawer)
    - JS: document click listener — if click target is not inside nav element, add "hidden" to mobile-menu (click outside closes)
    - scrollMarginTop compensation: in the script type="text/tailwindcss" config add: theme.extend.scrollMarginTop: { 'navbar': '4rem' } — and add class "scroll-mt-navbar" to each section placeholder (created in Task 3)
  </action>

  <verify>
    <automated>grep -c "hamburger-btn" "D:/Dự Án Claude/Web Kytoo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - nav element has classes: fixed top-0 left-0 right-0 z-50 h-16 bg-kytoo-bg border-b border-kytoo-border
    - Centered logo element exists with font-display font-bold uppercase text containing "KYTOO"
    - Left nav group has anchor links with href="#san-pham" and href="#bo-suu-tap"
    - Right nav group has anchor links with href="#ve-kytoo" and href="#lien-he"
    - CTA anchor has class containing bg-kytoo-red and NO rounded-* class; text is "Mua ngay"
    - CTA anchor href is set to a placeholder value (not empty) — to be replaced with Shopee URL in Phase 3
    - button element with id="hamburger-btn" exists with class containing md:hidden
    - div with id="mobile-menu" exists with class containing md:hidden and hidden
    - Mobile menu contains anchor links for all 4 sections and the CTA
    - Script block at end of body contains click handler for hamburger-btn toggling mobile-menu hidden class
    - Script block contains click-outside handler closing mobile-menu
    - Script block contains link-click handler closing mobile-menu on nav link click
    - Opening index.html at 320px viewport width shows only hamburger icon, no horizontal scroll
    - Opening index.html at 768px+ viewport shows symmetric nav links with centered KYTOO text
    - No border-radius class (rounded-*) on the CTA button element
  </acceptance_criteria>

  <done>Fixed navbar renders correctly at all breakpoints — desktop shows symmetric layout with CTA, mobile shows hamburger that opens/closes slide-down drawer</done>
</task>

<task type="auto">
  <name>Task 3: Empty section placeholders and minimal footer</name>

  <read_first>
    - CLAUDE.md — Section padding py-20 desktop / py-12 mobile, max-w-7xl mx-auto px-4; Section headings with red accent line; Footer pattern: bg #0F1923, border-top #2a3f52
    - .planning/phases/01-core-layout-hero/01-CONTEXT.md — decisions D-13 (4 section IDs, min-height, heading placeholders), D-14 (footer: copyright, border-top, nền tối)
  </read_first>

  <files>index.html</files>

  <action>
    Add the following structure inside body, after the nav and before the closing script tag. Use pt-16 on the first element after nav to compensate for the fixed navbar height.

    Hero placeholder (will be filled in Plan 01-02 — Task 1 of that plan replaces this):
    - section with id="hero" class="min-h-screen bg-kytoo-bg" — empty, just a height placeholder so the page has scroll space

    Section placeholders (per D-13) — 4 sections each with scroll-mt-navbar class:

    Section: id="san-pham" class="py-20 bg-kytoo-bg scroll-mt-navbar"
    - Inner: div class="max-w-7xl mx-auto px-4"
    - Placeholder heading: div with class="flex items-center gap-2 mb-4" containing: span class="w-8 h-0.5 bg-kytoo-red inline-block" and h2 class="text-3xl font-display font-bold uppercase tracking-widest text-kytoo-cream" with text "Sản Phẩm"
    - Below heading: p class="text-kytoo-gray text-sm" with text "— Phase 2 content —"

    Section: id="bo-suu-tap" class="py-20 bg-kytoo-bg-alt scroll-mt-navbar"
    - Same inner structure with heading "Bộ Sưu Tập" and placeholder text "— Phase 2 content —"

    Section: id="ve-kytoo" class="py-20 bg-kytoo-bg scroll-mt-navbar"
    - Same inner structure with heading "Về KYTOO" and placeholder text "— Phase 2 content —"

    Section: id="lien-he" class="py-20 bg-kytoo-bg-alt scroll-mt-navbar"
    - Same inner structure with heading "Liên Hệ" and placeholder text "— Phase 2 content —"

    Footer (per D-14):
    - footer element with class="bg-kytoo-bg border-t border-kytoo-border py-8"
    - Inner: div class="max-w-7xl mx-auto px-4 text-center"
    - p element: class="text-kytoo-gray text-sm" with text "© 2024 KYTOO Store. All rights reserved."

    Claude's Discretion — section min-heights: Use py-20 (80px top+bottom padding) as min visual height for placeholders. No explicit min-h needed since Phase 2 content will fill them.
  </action>

  <verify>
    <automated>grep -c "id=\"san-pham\"" "D:/Dự Án Claude/Web Kytoo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - index.html contains exactly these section id attributes: id="san-pham", id="bo-suu-tap", id="ve-kytoo", id="lien-he"
    - All 4 sections have class containing scroll-mt-navbar
    - id="san-pham" section has class containing bg-kytoo-bg; id="bo-suu-tap" has bg-kytoo-bg-alt (alternating per CLAUDE.md)
    - Each section contains an h2 with font-display font-bold uppercase and a red accent span (class "w-8 h-0.5 bg-kytoo-red")
    - footer element exists with class containing bg-kytoo-bg border-t border-kytoo-border
    - footer contains text "© 2024 KYTOO Store. All rights reserved."
    - footer text color uses kytoo-gray class
    - Clicking navbar anchor "#san-pham" smooth-scrolls to section without content hiding behind navbar (scroll-mt-navbar compensation works)
    - Page opens at 320px with no horizontal scrollbar
  </acceptance_criteria>

  <done>4 section placeholders with correct IDs and a minimal footer exist — navbar anchor links scroll correctly to each section with fixed navbar offset compensation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → browser | Tailwind CDN and Google Fonts load from third-party servers |
| User → navbar links | Anchor links navigate within page or to external Shopee URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Tailwind CDN (cdn.tailwindcss.com) | accept | Official CDN; no integrity hash needed for this project scale — static site with no user data |
| T-01-02 | Tampering | Google Fonts CDN | accept | Read-only CSS/font delivery; no script execution; low risk for static landing page |
| T-01-03 | Information Disclosure | Shopee placeholder href | accept | Placeholder — no real PII or credentials; replaced in Phase 3 with public shop URL |
| T-01-SC | Tampering | No npm/pip/cargo installs in this phase | accept | Phase 1 is pure HTML — no package manager involved; no slopcheck required |
</threat_model>

<verification>
Run these checks after plan execution completes:

1. File exists: index.html at project root
2. Tailwind CDN present: grep cdn.tailwindcss.com index.html
3. Custom colors defined: grep kytoo-red index.html (should appear in tailwind.config)
4. All 4 section IDs: grep -E "id=\"(san-pham|bo-suu-tap|ve-kytoo|lien-he)\"" index.html (4 matches)
5. Footer text: grep "KYTOO Store" index.html
6. Hamburger JS: grep "hamburger-btn" index.html
7. No rounded classes on CTA: verify CTA button element has no rounded-* class
8. Browser test: open index.html at 320px — no horizontal scroll, dark background visible, hamburger icon present
9. Browser test: open at 1024px — symmetric navbar with 2 links left, logo center, 2 links + CTA right
</verification>

<success_criteria>
- index.html exists and is valid HTML
- Browser renders #0F1923 background with no white flash
- Custom Tailwind tokens (kytoo-red, kytoo-bg, etc.) usable in subsequent plans
- Rajdhani Bold and Inter load from Google Fonts
- Navbar: fixed, h-16, symmetric desktop layout, functional mobile hamburger
- 4 section placeholders with IDs matching navbar anchor hrefs
- Footer with copyright text and dark styling
- Zero horizontal scroll at 320px viewport
</success_criteria>

<output>
Create `.planning/phases/01-core-layout-hero/01-01-SUMMARY.md` when done
</output>
