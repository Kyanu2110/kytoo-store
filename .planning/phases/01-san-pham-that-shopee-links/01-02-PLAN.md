---
phase: "01-san-pham-that-shopee-links"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - index.html
autonomous: false
requirements:
  - PROD-01
  - PROD-02

must_haves:
  truths:
    - "Không còn href='#' nào trên bất kỳ Shopee CTA nào — tất cả phải là URL thật hoặc có TODO rõ ràng"
    - "Không còn chữ 'https://shopee.vn' generic placeholder — tất cả đã được thay bằng URL shop thật"
    - "Tất cả img product tags có onerror attribute đúng pattern (tối thiểu 15)"
    - "Tên sản phẩm placeholder đã xóa hoàn toàn"
    - "Section #all-products tồn tại với đúng 15 product cards"

  artifacts:
    - path: "index.html"
      provides: "Final verified version with no remaining placeholders"
      contains: "shopeeLink:"
    - path: ".planning/phases/01-san-pham-that-shopee-links/01-01-SUMMARY.md"
      provides: "Summary of Plan A completion"
    - path: ".planning/phases/01-san-pham-that-shopee-links/01-02-SUMMARY.md"
      provides: "Summary of Phase 1 completion"

  key_links:
    - from: "All Shopee CTA anchors"
      to: "Real shopee.vn/shop/... URLs"
      via: "href attribute"
      pattern: "shopee\\.vn"

user_setup: []
---

<objective>
Audit and fix any remaining placeholder content, broken Shopee links, or structural gaps left after Plan A. Ensure Phase 1 passes all acceptance criteria before marking the phase complete.

Purpose: Quality gate — catch anything missed in the product replacement pass and ensure the deliverable (real products with clickable Shopee links) is fully working.

Output:
- Zero `href="#"` occurrences on any Shopee CTA (or remaining ones have explicit TODO comment explaining why)
- Zero `https://shopee.vn` generic placeholder URLs (all replaced with real shop/product URLs)
- All img product tags have correct onerror attribute
- No placeholder product names remain
- Phase 1 SUMMARY created
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-san-pham-that-shopee-links/01-CONTEXT.md
@.planning/phases/01-san-pham-that-shopee-links/01-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and patch remaining Shopee link gaps</name>

  <read_first>
    - index.html (full file — read current state after Plan A changes)
    - .planning/phases/01-san-pham-that-shopee-links/01-CONTEXT.md (D-07 through D-10 for link mapping rules)
    - .planning/phases/01-san-pham-that-shopee-links/01-01-SUMMARY.md (what was done in Plan A)
  </read_first>

  <files>index.html</files>

  <action>
Run a systematic audit of index.html for remaining issues. Fix each category:

**AUDIT 1 — Generic Shopee placeholder URLs:**
Search for `https://shopee.vn` that is NOT a real shop URL (i.e., bare `https://shopee.vn` with nothing after it, or `https://shopee.vn/` with nothing more). These are the original placeholder values from SHOPEE LINK slots ① ② ⑧. Per D-08, these general CTAs must point to the real shop main page URL (provided by user in Plan A Task 1).

Command to find them: grep for `href="https://shopee.vn"` or `href="https://shopee.vn/"` in index.html. For each found, replace with the real shop URL from user data.

The slots to check:
- SHOPEE LINK ① (navbar desktop + mobile): should be shop main URL
- SHOPEE LINK ② (#new-products "Xem tất cả"): should be shop main URL
- SHOPEE LINK ⑧ (gallery "Xem tất cả"): should be shop main URL

**AUDIT 2 — href="#" on product-specific Shopee CTAs:**
Search for `href="#"` on `<a>` tags that have Shopee-related content (target="_blank", rel="noopener", or Shopee text). Per D-09, per-product buttons must have a real listing URL.

Find all occurrences: look for `<a href="#"` near `MUA TRÊN SHOPEE` or `MUA NGAY` or `shopee` text. For each found:
- If the user provided a listing URL for this product in Task 1 data → fill it in
- If the user did NOT provide a URL for this specific product → add `<!-- TODO SHOPEE LINK: [product name] -->` comment and keep `href="#"` temporarily

**AUDIT 3 — onerror attribute on all product img tags:**
Search for `<img` tags in the product sections (#new-products, #featured, #collection). Each must have `onerror="this.style.display='none'; this.parentElement.style.background='linear-gradient(135deg,#0e1a28,#1a2a3e)'"`.

Exact onerror string to use (D-12):
`onerror="this.style.display='none'; this.parentElement.style.background='linear-gradient(135deg,#0e1a28,#1a2a3e)'"`

If any product img tag is missing this attribute, add it.

**AUDIT 4 — Old placeholder product names:**
Search for any remaining occurrence of: "Jett Windslash", "Reyna Devour", "Reyna Premium Keycap", "Sage Art Print", "Neon Mug", "Killjoy Mousepad", "Operator Skin Model Figurine", "REYNA DEVOUR", "JETT WINDSLASH", "SAGE HEALING", "NEON SPRINT", "KILLJOY LAB". If any found, replace with the real product name from the user's data.

**AUDIT 5 — agent-buy-btn id:**
Confirm the "MUA TRÊN SHOPEE" anchor in the agent info panel (SHOPEE LINK ⑦, line ~780) has `id="agent-buy-btn"`. If missing, add it. This is required for the switchAgent() function to update the href dynamically.

**AUDIT 6 — featTotal JS variable:**
Confirm `featTotal` in the JS (line ~1449) matches the actual number of `.feat-slide` elements in #feat-track. If user added more than 3 products to the carousel, this variable must be updated and feat-dot buttons added accordingly. Check: count `<div class="feat-slide">` occurrences and compare to `featTotal = N`.

**For each fix made, add an inline comment**: `<!-- PHASE1-FIXED: [brief reason] -->` on the line immediately above the fixed element, so the executor's changes are traceable.
  </action>

  <verify>
    <automated>
    1. No bare generic Shopee placeholder:
       grep -c 'href="https://shopee.vn"' index.html
       (should be 0 — all replaced with real shop URL)

    2. Count remaining href="#" near Shopee content (should be 0 or documented as TODO):
       grep -n 'href="#"' index.html | grep -i "shopee\|mua\|noopener"

    3. All product img tags have onerror:
       grep -c "onerror=" index.html
       (should be at least 15, matching product count)

    3b. #all-products section exists with 15 cards:
       grep -c "id=\"all-products\"" index.html
       (should be 1)

    4. No old placeholder names:
       grep -ic "jett windslash\|reyna devour\|sage healing\|neon sprint\|killjoy lab\|operator skin model" index.html
       (should be 0)

    5. agent-buy-btn id present:
       grep -c 'id="agent-buy-btn"' index.html
       (should be 1)
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'href="https://shopee.vn"' index.html` returns 0 (no bare generic placeholder URLs)
    - `grep -ic "jett windslash\|reyna devour\|sage healing\|neon sprint\|killjoy lab" index.html` returns 0
    - `grep -c "onerror=" index.html` returns a number equal to the product count (at least 15)
    - `grep -c 'id="agent-buy-btn"' index.html` returns 1
    - `grep -c "featTotal" index.html` returns 1 and its value matches the actual feat-slide count
    - Any remaining `href="#"` on Shopee CTAs has a `<!-- TODO SHOPEE LINK: [product name] -->` comment directly above it
  </acceptance_criteria>

  <done>
    All Shopee link slots are either filled with real URLs or explicitly marked as TODO. No generic placeholder URLs remain. All product img tags have onerror. No placeholder names remain.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Final browser verification — phase complete sign-off</name>

  <read_first>
    - index.html (to understand the current state for any last fixes)
    - .planning/ROADMAP.md (Phase 1 success criteria to verify against)
  </read_first>

  <files>index.html</files>

  <what-built>
    Complete Phase 1 implementation: all placeholder content replaced with real KYTOO products, all Shopee links wired, all img onerror fallbacks in place, all structural changes (aspect-square) applied.
  </what-built>

  <how-to-verify>
Open index.html in a browser (double-click file, or use Live Server if available). Check all 3 ROADMAP Phase 1 success criteria:

**Success Criterion 1** — "Tất cả sản phẩm trong featured carousel, agent tabs, và collection slider có ảnh thật (hoặc đúng slot để user thêm ảnh)":
- Featured carousel: click left/right arrows — each slide shows a real product name and either a real image or a dark gradient (not a letter placeholder)
- Agent Gear tabs (#featured): click each tab — right panel shows real product name, price, description
- Collection slider (#collection): left/right arrows navigate between real product groupings

**Success Criterion 2** — "Tất cả nút 'Mua ngay' / 'Xem trên Shopee' có link thật đến Shopee listing":
- Click navbar "MUA NGAY" button — opens Shopee in new tab (not a 404 or generic shopee.vn homepage)
- Click featured carousel "MUA TRÊN SHOPEE" on at least 2 slides — opens correct Shopee listing
- Click #featured "MUA TRÊN SHOPEE" button — opens correct Shopee listing for the active tab's product
- Click footer "Shopee Store" link — opens shop main page on Shopee

**Success Criterion 3** — "Tên và giá sản phẩm khớp với Shopee":
- Compare product names and prices shown on page vs. actual Shopee listings — should match

If any Shopee link opens to `about:blank`, an error page, or the generic Shopee homepage when it should be a specific listing — report it as an issue.
  </how-to-verify>

  <action>
If the human reviewer reports issues:
1. Re-read index.html to find the specific href value for the reported button
2. Fix the href to use the correct URL (ask user for the correct URL if not already provided)
3. Re-run the automated checks from Task 1
4. Present for re-verification

If all checks pass, do nothing — proceed to write the SUMMARY.
  </action>

  <resume-signal>Type "phase 1 complete" if all 3 success criteria pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopee URLs in href attributes | User-provided URLs are set at build time; no runtime user input constructs these values |
| onerror inline JS | Executes in browser when img fails to load; must remain a safe, hardcoded literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | href audit patching | mitigate | Executor must verify each patched href starts with `https://shopee.vn/` before saving; no constructed URL strings allowed — must be literal quoted values |
| T-02-02 | Tampering | onerror attribute additions | mitigate | Executor uses the exact literal string from D-12 verbatim — no variation; `grep -c "onerror=" index.html` must return exactly N (product count) after Task 1, not more |
| T-02-03 | Tampering | PHASE1-FIXED comment injection | accept | Comments are non-executable HTML; accepted risk is trivial since the file is developer-controlled |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — static HTML only |
</threat_model>

<verification>
Phase 1 fully verified when:
1. All 3 ROADMAP Phase 1 success criteria confirmed by human in Task 2
2. Automated audit grep checks from Task 1 all pass (counts match expected values)
3. At least one real Shopee link successfully opens a KYTOO product listing in browser
</verification>

<success_criteria>
- Zero generic `https://shopee.vn` placeholder href values remaining in index.html
- Zero old placeholder product names (Jett Windslash Hoodie, Reyna Devour Keycap, Sage Healing, Neon Sprint, Killjoy Lab) in index.html
- Count of `onerror=` attributes equals product img tag count (tối thiểu 15)
- Section #all-products tồn tại và chứa đúng 15 product cards
- All 3 ROADMAP Phase 1 success criteria verified by human sign-off
- Phase 1 status updated to "Complete" in STATE.md
</success_criteria>

<output>
Create `.planning/phases/01-san-pham-that-shopee-links/01-02-SUMMARY.md` when done.
Update `.planning/STATE.md` to mark Phase 1 as Complete.
Update `.planning/ROADMAP.md` to mark Phase 1 status as Complete and tick off PROD-01, PROD-02.
Update `.planning/REQUIREMENTS.md` to mark PROD-01 and PROD-02 as complete: `- [x] **PROD-01**` and `- [x] **PROD-02**`.
</output>
